En javascript, existen varias formas de declarar funciones. aquí te presento las más comunes: 1. declaración de función con la palabra clave function la forma más común de declarar una función en javascript es utilizando la palabra clave function. esta forma de declarar una función se conoce como declaración de función. la sintaxis es la siguiente: "function nombredelafuncion(parametro1, parametro2, , parametron) { // cuerpo de la función return resultado; }" 2. expresión de función otra forma de declarar una función es mediante una expresión de función. en este caso, se utiliza la palabra clave var, let o const para declarar una variable que contendrá la función. la sintaxis es la siguiente: "var nombredelafuncion = function(parametro1, parametro2, , parametron) { // cuerpo de la función return resultado; }" 3. función flecha a partir de es6, se introdujo una nueva forma de declarar funciones llamada función flecha o arrow function. esta forma de declarar una función es una sintaxis más concisa y legible que las formas anteriores. la sintaxis es la siguiente: "const nombredelafuncion = (parametro1