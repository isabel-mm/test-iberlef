Aquí te dejo un ejemplo de cómo crear una aplicación de consola en .net utilizando c# y mostrar una barra de progreso: using system; namespace consoleprogressbar { class program { static void main(string() args) { console.write("procesando "); for (int i = 0; i < 100; i++) { console.write("("); for (int j = 0; j < i; j++) { console.write("#"); } for (int j = i; j < 100; j++) { console.write("."); } console.write(") {0}% ", i + 1); system.threading.thread.sleep(100); } console.writeline(" proceso completado."); console.readline(); } } } este ejemplo muestra una barra de progreso que avanza de 1 en 1 hasta llegar al 100%. la barra se actualiza cada 100 milisegundos y se utiliza el carácter "#" para representar la parte completada de la barra y el carácter "." para representar la parte que falta. la barra se muestra en la misma línea utilizando el carácter de retorno de carro "