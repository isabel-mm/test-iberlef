Claro, aquí tienes un código en r que muestra los histogramas de las variables del dataset "cars": # cargamos el dataset "cars" en r data("cars") # mostramos los histogramas de las variables par(mfrow = c(2, 2)) hist(cars$speed) hist(cars$dist) # añadimos títulos a los histogramas title("histograma de la variable "speed"") title("histograma de la variable "dist"") este código carga el dataset "cars" en r y luego muestra dos histogramas, uno para la variable "speed" y otro para la variable "dist". utilizamos la función par para especificar que queremos mostrar dos histogramas por fila, y la función title para añadir títulos a los