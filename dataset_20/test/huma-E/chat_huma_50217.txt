Dada una matriz bidimensional (de dimensiones arbitrarias n x m), en cuyas celdas puede haber 0s o 1s aleatoriamente distribuidos, generar una función en javascript que detecta si existe alguna cruz de 1s en la cual una columna completa y una fila completa contengan únicamente 1s. la función ha de retornar false si no existe una combinación de fila y columna que cumplan esa condición, y retornar un array con los índices de fila y columna en la que se encuentra la primera ocurrencia de fila y columna que solo contienen 1s, de la forma: (n,m). si existe más de una cruz es irrelevante, cualquiera de las cruces existentes se considerarán una respuesta válida. adicionalmente, ¿sería posible implementar esa función con un solo bucle "for"? generar el código javascript y algunas matrices de prueba para poder comprobar que los resultados son los